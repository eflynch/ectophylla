class NoteSphere(object):
	def __init__(self, pitch, velocity, duration, start_pos, end_pos, time, tick):
		self.pitch = pitch
		self.velocity = velocity
		self.duration = duration
		self.start_pos = start_pos
		self.end_pos = end_pos
		self.time = time
		self.tick = tick

	