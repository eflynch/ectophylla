class Note(object):
    def __init__(self, pitch, velocity, duration, x, y, tp, speed):
        self.pitch = pitch
        self.velocity = velocity
        self.duration = duration
        self.x = x
        self.y = y
        self.tp = tp
        self.speed = speed
